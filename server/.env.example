# ═══════════════════════════════════════════════════════════════════
#  TZVETOMIR.DEV — Server Environment Variables
#  Copy this to .env and fill in your values:
#    cp .env.example .env
# ═══════════════════════════════════════════════════════════════════

# ─── Database ────────────────────────────────────────────────────
# PostgreSQL connection string.
# For Neon (recommended): Get this from your Neon dashboard
# For Railway PostgreSQL: Railway auto-generates this
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
DATABASE_URL="postgresql://username:password@host:5432/tzvetomir_portfolio?sslmode=require"

# ─── Server ──────────────────────────────────────────────────────
# Port (Railway sets this automatically; local default is 3001)
PORT=3001

# Node environment
NODE_ENV=development

# ─── CORS ────────────────────────────────────────────────────────
# Comma-separated list of allowed frontend origins.
# In production: https://tzvetomir.dev,https://www.tzvetomir.dev
# In development: http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173

# ─── Email Service (Optional — for newsletter & contact form) ───
# Uncomment and fill in when you're ready to send emails.
# Resend is recommended (https://resend.com — 3K emails/month free).
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
